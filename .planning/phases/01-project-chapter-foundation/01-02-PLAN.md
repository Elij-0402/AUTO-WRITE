---
phase: 01-project-chapter-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/db/chapter-queries.ts
  - src/lib/hooks/use-chapters.ts
  - src/lib/hooks/use-autosave.ts
  - src/app/projects/[id]/layout.tsx
  - src/app/projects/[id]/page.tsx
  - src/components/chapter/chapter-sidebar.tsx
  - src/components/chapter/chapter-row.tsx
  - src/components/chapter/chapter-context-menu.tsx
  - src/components/chapter/create-chapter-input.tsx
  - src/components/chapter/delete-chapter-dialog.tsx
  - src/components/project/project-settings-form.tsx
autonomous: true
requirements:
  - PROJ-03
  - PROJ-04
  - L10N-01

must_haves:
  truths:
    - "User can see a chapter sidebar when opening a project"
    - "User can create a new chapter inline at the bottom of the sidebar"
    - "User can drag-reorder chapters and the order persists"
    - "User can rename, delete, and duplicate chapters via context menu"
    - "Chapter numbering auto-generates with 第N章 prefix"
    - "All chapter management UI is in Simplified Chinese"
  artifacts:
    - path: "src/components/chapter/chapter-sidebar.tsx"
      provides: "Sortable chapter list sidebar"
      contains: "SortableContext"
    - path: "src/components/chapter/chapter-row.tsx"
      provides: "Individual chapter row with drag handle"
      contains: "第"
    - path: "src/components/chapter/create-chapter-input.tsx"
      provides: "Inline chapter creation input"
      contains: "新章节"
    - path: "src/app/projects/[id]/layout.tsx"
      provides: "Project workspace layout with sidebar"
      min_lines: 20
  key_links:
    - from: "src/app/projects/[id]/page.tsx"
      to: "src/components/chapter/chapter-sidebar.tsx"
      via: "React import and render in workspace layout"
      pattern: "ChapterSidebar"
    - from: "src/components/chapter/chapter-sidebar.tsx"
      to: "src/lib/hooks/use-chapters.ts"
      via: "Custom hook for chapter CRUD"
      pattern: "useChapters"
    - from: "src/lib/hooks/use-chapters.ts"
      to: "src/lib/db/chapter-queries.ts"
      via: "Dexie.js chapter queries"
      pattern: "chapter\\.(add|put|bulkPut)"
    - from: "src/components/chapter/chapter-sidebar.tsx"
      to: "@dnd-kit/sortable"
      via: "Drag and drop sortable context"
      pattern: "DndContext|SortableContext"
---

<objective>
Build the project workspace with chapter sidebar, implementing full chapter CRUD with drag-reorder and Chinese-first UI.

Purpose: Complete the author's ability to organize their novel by managing chapters within a project — creating, renaming, reordering, and deleting chapters with a sidebar that's always visible alongside the editor area (editor itself comes in Phase 2).

Output: Project workspace page at /projects/[id] with sortable chapter sidebar, inline chapter creation, context menu actions, all in Simplified Chinese.
</objective>

<execution_context>
@D:/AUTO-WRITE/.opencode/get-shit-done/workflows/execute-plan.md
@D:/AUTO-WRITE/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-chapter-foundation/01-CONTEXT.md
@.planning/phases/01-project-chapter-foundation/01-RESEARCH.md
@.planning/phases/01-project-chapter-foundation/01-VALIDATION.md

<!-- Types from Plan 01 that this plan depends on -->
<interfaces>
From src/lib/types/project.ts (created in Plan 01):
```typescript
export interface ProjectMeta {
  id: string;
  title: string;
  genre: string;
  synopsis: string;
  coverImageId: string | null;
  wordCount: number;
  createdAt: Date;
  updatedAt: Date;
  deletedAt: Date | null;
}

export interface CreateProjectInput {
  title: string;
  genre?: string;
  synopsis?: string;
}
```

From src/lib/types/chapter.ts (created in Plan 01):
```typescript
export type ChapterStatus = 'draft' | 'completed';

export interface Chapter {
  id: string;
  projectId: string;
  order: number;
  title: string;
  content: object | null;
  wordCount: number;
  status: ChapterStatus;
  createdAt: Date;
  updatedAt: Date;
  deletedAt: Date | null;
}
```

From src/lib/db/meta-db.ts (created in Plan 01):
```typescript
export const metaDb: InkForgeMetaDB; // singleton with .projectIndex table
```

From src/lib/db/project-db.ts (created in Plan 01):
```typescript
export function createProjectDB(projectId: string): InkForgeProjectDB;
// InkForgeProjectDB has .projects and .chapters tables
```

From src/lib/hooks/use-projects.ts (created in Plan 01):
```typescript
export function useProjects(): {
  projects: ProjectMeta[];
  createProject: (input: CreateProjectInput) => Promise<string>;
  updateProject: (id: string, data: Partial<ProjectMeta>) => Promise<void>;
  softDeleteProject: (id: string) => Promise<void>;
  restoreProject: (id: string) => Promise<void>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chapter data layer + auto-save hook + project workspace layout</name>
  <files>src/lib/db/chapter-queries.ts, src/lib/hooks/use-chapters.ts, src/lib/hooks/use-autosave.ts, src/app/projects/[id]/layout.tsx, src/app/projects/[id]/page.tsx</files>
  <read_first>
    - src/lib/types/chapter.ts (Chapter interface and ChapterStatus type)
    - src/lib/db/project-db.ts (createProjectDB factory function)
    - src/lib/db/meta-db.ts (metaDb singleton)
    - src/lib/hooks/use-projects.ts (useProjects hook pattern to follow)
    - .planning/phases/01-project-chapter-foundation/01-CONTEXT.md (D-09, D-14, D-16, D-21, D-23 through D-27)
  </read_first>
  <behavior>
    - Test 1: Adding a chapter to a project stores it with correct fields and auto-incremented order
    - Test 2: Reordering chapters updates the `order` field for all affected chapters
    - Test 3: Soft-deleting a chapter sets deletedAt, it's excluded from default queries
    - Test 4: Duplicating a chapter creates a copy with "(副本)" appended to title
    - Test 5: Chapter numbering derives from order position: order 0 → "第1章", order 1 → "第2章"
    - Test 6: Auto-save hook fires on content change after 500ms debounce
  </behavior>
  <action>
    1. **Create chapter query functions** in `src/lib/db/chapter-queries.ts`:
       - These are pure async functions that operate on an InkForgeProjectDB instance (received as parameter).
       - `getChapters(db: InkForgeProjectDB)`: Returns all non-deleted chapters ordered by `order` ascending. Uses Dexie's `useLiveQuery` compatible pattern.
       - `addChapter(db: InkForgeProjectDB, projectId: string, title: string)`: Creates a new chapter with NanoID, sets order to max(order) + 1, status 'draft', null content. Per D-16: title is user-provided portion only; "第N章" prefix is computed at display time.
       - `reorderChapters(db: InkForgeProjectDB, chapterIds: string[])`: Updates `order` field for all chapters based on new position in array. Runs in a Dexie transaction for atomicity.
       - `renameChapter(db: InkForgeProjectDB, id: string, title: string)`: Updates title and updatedAt.
       - `softDeleteChapter(db: InkForgeProjectDB, id: string)`: Sets deletedAt per D-15 and D-27. After deletion, reorder remaining chapters to close the gap.
       - `duplicateChapter(db: InkForgeProjectDB, id: string)`: Creates a copy of the chapter with title "{originalTitle}（副本）", placed after the original in order, status 'draft'.
       - `updateChapterContent(db: InkForgeProjectDB, id: string, content: object)`: Updates content and wordCount. Per D-25: word count is precomputed (count all characters minus whitespace for Chinese text).
       - `updateChapterStatus(db: InkForgeProjectDB, id: string, status: ChapterStatus)`: Updates status and updatedAt.
       - `getChapterNumber(order: number)`: Returns "第{order+1}章" per D-16 (1-indexed display).

    2. **Create useChapters hook** in `src/lib/hooks/use-chapters.ts`:
       - Accepts `projectId: string` parameter
       - Opens project DB via `createProjectDB(projectId)` — use `useMemo` to avoid recreating on every render
       - Uses `useLiveQuery` from `dexie-react-hooks` for reactive chapter list
       - Exposes:
         - `chapters`: sorted non-deleted chapters array
         - `addChapter(title: string)`: calls chapter-queries.addChapter
         - `reorderChapters(chapterIds: string[])`: calls chapter-queries.reorderChapters
         - `renameChapter(id: string, title: string)`: calls chapter-queries.renameChapter
         - `softDeleteChapter(id: string)`: calls chapter-queries.softDeleteChapter
         - `duplicateChapter(id: string)`: calls chapter-queries.duplicateChapter
         - `updateChapterStatus(id: string, status: ChapterStatus)`: calls chapter-queries.updateChapterStatus
       - Also updates the project's updatedAt in metaDb whenever chapters change (so project card shows correct "last edited" time)

    3. **Create useAutoSave hook** in `src/lib/hooks/use-autosave.ts` per D-21:
       - Parameters: `saveFn: () => Promise<void>`, `deps: any[]`, `debounceMs: number = 500`
       - On content change (tracked via deps), debounces and calls saveFn
       - On chapter switch (component unmount), calls saveFn immediately
       - On window blur (`visibilitychange` event), calls saveFn immediately
       - Returns `{ isSaving: boolean, lastSaved: Date | null }`

    4. **Create project workspace layout** in `src/app/projects/[id]/layout.tsx`:
       - Mark as `'use client'`
       - Accept `[id]` param from Next.js
       - Load project metadata using `useProjects` hook (filter by id)
       - Render layout with:
         - Top bar: project title + "返回" (Back) button to dashboard
         - Left sidebar area (ChapterSidebar will be added in Task 2)
         - Main content area (placeholder: "选择一个章节开始写作" / "Select a chapter to start writing")
       - Handle 404 case: if project not found, show "项目未找到" (Project not found)

    5. **Create project workspace page** in `src/app/projects/[id]/page.tsx`:
       - Mark as `'use client'`
       - Render ChapterSidebar in the sidebar area
       - Main content shows placeholder text until Phase 2 adds the editor
       - For now, main content shows selected chapter's title and status if a chapter is selected

    6. **Install @dnd-kit dependencies** (needed for Task 2 but install now):
       ```bash
       npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
       ```
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - Chapter CRUD operations work via chapter-queries.ts
    - useChapters hook provides reactive chapter list sorted by order
    - Reorder updates order fields atomically in a Dexie transaction
    - Soft delete with deletedAt works and reorders remaining chapters
    - Duplicate creates copy with （副本）suffix
    - getChapterNumber(0) returns "第1章", getChapterNumber(4) returns "第5章"
    - useAutoSave hook debounces on content change, saves on blur and unmount
    - Project workspace layout renders at /projects/[id]
    - @dnd-kit packages installed
  </done>
</task>

<task type="auto">
  <name>Task 2: Chapter sidebar UI with drag-reorder, context menu, inline creation — all Simplified Chinese</name>
  <files>src/components/chapter/chapter-sidebar.tsx, src/components/chapter/chapter-row.tsx, src/components/chapter/chapter-context-menu.tsx, src/components/chapter/create-chapter-input.tsx, src/components/chapter/delete-chapter-dialog.tsx</files>
  <read_first>
    - src/lib/hooks/use-chapters.ts (useChapters hook API)
    - src/lib/db/chapter-queries.ts (getChapterNumber function)
    - src/lib/types/chapter.ts (Chapter interface)
    - src/components/ui/dialog.tsx (Dialog component from Plan 01)
    - src/components/ui/button.tsx (Button component from Plan 01)
    - .planning/phases/01-project-chapter-foundation/01-CONTEXT.md (D-09 through D-16 for chapter UI decisions)
  </read_first>
  <action>
    1. **Create ChapterRow component** in `src/components/chapter/chapter-row.tsx` per D-10:
       - Uses `useSortable` from @dnd-kit/sortable for drag support
       - Displays:
         - Drag handle (⋮⋮ icon, grip vertical) on the left
         - Chapter number: computed via getChapterNumber(chapter.order) — e.g., "第1章" (D-16)
         - Chapter title (truncated if long, with ellipsis)
         - Word count badge: "{wordCount}字" (D-10)
         - Status badge: "草稿" (draft) or "已完成" (completed) in Chinese (D-10)
         - Three-dot menu button (⋯) that opens ChapterContextMenu
       - Selected state: highlighted background when this chapter is the active chapter
       - Click on row (not handle/menu): selects this chapter as active
       - IME safety: no actions triggered during composition events on the title

    2. **Create ChapterContextMenu component** in `src/components/chapter/chapter-context-menu.tsx` per D-13:
       - Uses @radix-ui/react-dropdown-menu (installed in Plan 01)
       - Menu items:
         - "重命名" (Rename) — switches ChapterRow to inline edit mode
         - "复制" (Duplicate) — calls useChapters.duplicateChapter()
         - "标记为已完成" / "标记为草稿" (Mark as Completed/Draft) — toggles status
         - Separator line
         - "删除" (Delete) — opens DeleteChapterDialog
       - All items with Lucide icons
       - All text in Simplified Chinese

    3. **Create DeleteChapterDialog component** in `src/components/chapter/delete-chapter-dialog.tsx` per D-15:
       - Confirmation dialog using Dialog from ui/
       - Text: "确定要删除「{chapter.title}」吗？" (Are you sure you want to delete "{title}"?)
       - Subtitle: "删除后可在回收站中恢复" (Can be restored from trash)
       - Confirm button: "删除" (Delete) — danger/red style
       - Cancel button: "取消" (Cancel)

    4. **Create CreateChapterInput component** in `src/components/chapter/create-chapter-input.tsx` per D-11:
       - Renders at the bottom of the chapter list
       - Shows a button "+ 新章节" (+ New Chapter) that expands to an input field
       - Input placeholder: "输入章节标题" (Enter chapter title)
       - On Enter: calls useChapters.addChapter(title), clears input, collapses back to button
       - On Escape: clears input, collapses back to button
       - IME safety: only submit on Enter when NOT in composition (check `isComposing` on keyboard event)
       - After creating a chapter, it appears in the list and auto-selects as the active chapter

    5. **Create ChapterSidebar component** in `src/components/chapter/chapter-sidebar.tsx` per D-09, D-12:
       - Accepts `projectId: string` and `activeChapterId: string | null` and `onSelectChapter: (id: string) => void`
       - Uses `useChapters(projectId)` hook for data
       - Renders in a fixed-width sidebar (280px, per agent's discretion)
       - Contains:
         - Sidebar header: "章节" (Chapters) title + chapter count badge
         - Scrollable chapter list with DndContext + SortableContext wrapping ChapterRow components
         - CreateChapterInput at the bottom
       - Drag and drop implementation:
         - Wrap list in `DndContext` with `closestCenter` collision detection
         - Use `SortableContext` with `verticalListSortingStrategy`
         - On `onDragEnd`: extract new order from `arrayMove`, call `useChapters.reorderChapters(newOrderIds)`
         - Apply `restrictToVerticalAxis` modifier to constrain movement
         - Drag overlay: render a ghost ChapterRow during drag
       - Empty state: "还没有章节" (No chapters yet) with CreateChapterInput
       - Chapter selection: click sets activeChapterId (managed by parent page via useState)

    6. **Wire up project settings** — Extend the project workspace to include a settings access point:
       - Add "项目设置" (Project Settings) button in the workspace top bar
       - When clicked, shows ProjectSettingsForm (from Plan 01) in a dialog or replaces main content
       - This completes D-07 for the full settings page within workspace

    7. **Update project workspace page** to integrate the sidebar:
       - Import ChapterSidebar and render in sidebar area
       - Manage `activeChapterId` state with `useState`
       - Pass `onSelectChapter` callback to update activeChapterId
       - Main content area shows chapter info or placeholder
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - Chapter sidebar renders in project workspace at /projects/[id]
    - Chapter list shows rows with 第N章 prefix, title, 字数, status badge
    - Drag-reorder works: chapters can be dragged vertically and order persists
    - Three-dot menu shows rename, duplicate, status toggle, delete options
    - Inline chapter creation works at bottom of sidebar
    - Chapter deletion shows confirmation dialog
    - All text in Simplified Chinese (章节, 草稿, 已完成, 新章节, 重命名, 复制, 删除, 取消)
    - Active chapter is highlighted
    - Chapter numbers update after reorder
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Chapter data | Chapter titles and content are user-provided text stored in IndexedDB |
| Drag-reorder → Data integrity | Reorder operations must be atomic to prevent data corruption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | I | Chapter title rendering | mitigate | React JSX auto-escapes; verify no dangerouslySetInnerHTML. Chapter titles rendered as text content. |
| T-01-07 | D | Chapter reorder atomicity | mitigate | Use Dexie transaction in reorderChapters to prevent partial updates that could corrupt order. |
| T-01-08 | D | Auto-save data loss | mitigate | Save on window blur and chapter switch in addition to debounce. Log save failures to console with actionable message. |
</threat_model>

<verification>
1. Run `npx vitest run` — all tests pass
2. Navigate to /projects/{id} — workspace renders with chapter sidebar
3. Create chapters — they appear with 第N章 numbering
4. Drag a chapter — order updates and persists on refresh
5. Use context menu to rename, duplicate, delete chapters
6. Verify all text is Simplified Chinese
7. Check that chapter data persists after page refresh (IndexedDB)
</verification>

<success_criteria>
- Chapter sidebar renders in project workspace
- Chapters can be created inline with 第N章 auto-numbering
- Drag-reorder persists the new order to IndexedDB
- Context menu provides rename, duplicate, status toggle, delete
- Soft delete with confirmation dialog
- All chapter management UI in Simplified Chinese
- All data persists across page refresh
- All Vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-chapter-foundation/01-02-SUMMARY.md`
</output>
